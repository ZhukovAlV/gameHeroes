package com.company;

import javax.swing.*;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

/**
 * @author Alexey Zhukov
 * @version 03.10.2021
 */
public class Main {

    public static void main(String[] args) {/*
        Hero loki = new Hero();
        loki.name = "Локи";
        loki.age = 1500;

        loki.gender = 'м';
        loki.power = 60;
        loki.agility = 90;

        loki.superpower = "Превращение";

        // Это блок оружия
        Weapon weapon = new Weapon();
        weapon.name = "Магическое оружие (клон)";
        weapon.damage = 50;
        loki.weapon = weapon;

        System.out.println(loki.weapon.name);

        loki.weapon.shoot();
        loki.say();

        // Создается окно
        JFrame window = new JFrame();

        // Задаем отступ и размеры
        window.setBounds(0,0,1024,768);

        // Компонент для имени
        JButton labelName = new JButton();
        labelName.setText(loki.name);

        // Добавляю компонент в окно
        window.add(labelName);

        // Отобразить окно
        window.setVisible(true);


        labelName.addActionListener(e -> {
            System.out.println("Я молодец");
        });
*/
    }
}
